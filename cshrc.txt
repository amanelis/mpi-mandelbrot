set path=(/bin /usr/bin /usr/ucb /etc .)
setenv P4_RSHCOMMAND rsh
set path=( ~/bin ${USRLOCAL} ${JDK} ${ROPTPATH} ${SYSTEM} ${NEWSSTUFF} ${FACBIN} /home/maynard/bin $path)  
set path=( $path /usr/local/mpich-1.2.5/bin)
set path=( /usr/local/mpich-1.2.5/bin $path)
setenv LD_LIBRARY_PATH /usr/local/lib
setenv GLOBUS_LOCATION ~/globus2.0
set filec 
